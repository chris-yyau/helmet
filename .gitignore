
# macOS Finder metadata
.DS_Store
**/.DS_Store

# helmet drift-scan fleet list (operator-specific)
.helmet-fleet

# Claude Code / busdriver operational state (machine-local — never commit)
.claude/*.local
.claude/*.local.*
.claude/*.log
.claude/*.lock
.claude/litmus-state.md
.claude/bypass-log.jsonl
.claude/review-metrics.jsonl
.claude/ultra-oracle/
.claude/*-review-packet.md

# CodeGraph per-repo index (machine-local — daemon db, socket, pid, wal)
.codegraph/

# Multi-model review scratch (raw model dumps; curated follow-ups are filed as GitHub issues)
docs/reviews/
